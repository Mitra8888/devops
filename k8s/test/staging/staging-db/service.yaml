apiVersion: v1
kind: Service
metadata:
  name: staging-mongodb-service
  namespace: staging
  labels:
    app: staging-mongodb
spec:
  type: ClusterIP
  ports:
  - port: 27017
    targetPort: 27017
  selector:
    app: staging-mongodb