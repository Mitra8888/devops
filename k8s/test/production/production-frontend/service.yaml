apiVersion: v1
kind: Service
metadata:
  name: production-frontend-service
  namespace: production
  labels:
    app: production-frontend
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: production-frontend