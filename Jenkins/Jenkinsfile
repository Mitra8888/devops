pipeline {
    agent any
    
    tools {
        nodejs "NODEJS"
    }  
    
    stages {
        
        stage('Fetching code') {
            steps {
                git branch: 'main', url: 'https://github.com/Mitra8888/devops.git'
            }
        }
        
        stage('Unit testing') {
            steps {
                dir('frontend') {
                    sh 'npm ci'
                    sh 'npm test -- --watch=false --browsers=ChromeHeadless --no-progress'
                }
            }
        }
        
        stage('Build') {
            steps {
                dir('frontend') {
                    sh 'npm run build'
                }
            }
            post {
                success {
                    echo "Archiving artifact"
                    archiveArtifacts artifacts: 'frontend/build/**'
                }
            }
        }
    }
}