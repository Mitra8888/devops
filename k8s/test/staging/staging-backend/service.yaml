apiVersion: v1
kind: Service
metadata:
  name: staging-backend-service
  namespace: staging
  labels:
    app: staging-backend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
  selector:
    app: staging-backend