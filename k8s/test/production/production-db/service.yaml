apiVersion: v1 
kind: Service
metadata:
  name: production-mongodb-service
  namespace: production
  labels:
    app: production-mongodb
spec:
  type: ClusterIP
  ports:
  - port: 27017
    targetPort: 27017
  selector:
    app: production-mongodb