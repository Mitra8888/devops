apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: production-mongodb-pvc
  namespace: production
  labels:
    app: production-mongodb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi