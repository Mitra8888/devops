apiVersion: v1 
kind: Service 
metadata:
  name: staging-frontend-service
  namespace: staging
  labels:
    app: staging-frontend
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: staging-frontend